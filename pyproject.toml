[project]
name = "discord-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "discord>=2.3.2",
    "dotenv>=0.9.9",
    "pydub>=0.25.1",
    "pynacl>=1.5.0",
]

[dependency-groups]
dev = [
    "ipython>=8.12.3",
]
