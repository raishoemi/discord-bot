[project]
name = "discord-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "discord.py>=2.6.0",
    "python-dotenv>=1.0.0",
    "pydub>=0.25.1",
    "pynacl>=1.5.0",
]

[dependency-groups]
dev = [
    "ipython>=8.12.3",
]
